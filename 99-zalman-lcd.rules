# Access to the Zalman Alpha 2 display (0483:5740) without root.
# Installation:
#   sudo cp 99-zalman-lcd.rules /etc/udev/rules.d/
#   sudo udevadm control --reload && sudo udevadm trigger
#
# 1) The serial port itself: /dev/ttyACM*
SUBSYSTEM=="tty", ATTRS{idVendor}=="0483", ATTRS{idProduct}=="5740", MODE="0666", SYMLINK+="zalman_lcd"
# 2) USB device node — required for USBDEVFS_RESET (automatic stall recovery)
SUBSYSTEM=="usb", ATTR{idVendor}=="0483", ATTR{idProduct}=="5740", MODE="0666"
